# Lightweight runtime for deploying the READ-ONLY dashboard on Streamlit Community Cloud.
# The dashboard reads a precomputed snapshot (data/forecasts/forecasts.parquet), so it
# needs none of the modelling stack. The full project (training, DuckDB, conformal, etc.)
# installs from pyproject.toml + uv.lock instead.
streamlit==1.58.0
pandas==2.3.3
numpy==2.4.6
pyarrow==21.0.0
plotly==5.24.1
pydeck==0.9.2
